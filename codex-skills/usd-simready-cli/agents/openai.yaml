interface:
  display_name: "USD SimReady CLI"
  short_description: "Process USD assets for SimReady validation."
  default_prompt: "Use $usd-simready-cli for SimReady recommendation, mesh-gated process/apply, dependency packaging, scale/orientation correction, static collision authoring, safe RB.COL.002 candidate repair, and report verification. For RB.COL.002, accept only primitive_collider_audit.json from omni-asset-cli physics-collider-audit, export a separate candidate through collider-repair, and preserve CollisionAPI, geometry, transforms, rigid bodies, and joints; then ask omni-asset-cli to re-audit. Let process call the validator preflight before authoring; for standalone validator explanation, Isaac Sim runtime, or simready-flywheel reports, use $omniverse-usd-asset-validator. If downstream runtime output disagrees on size, verify the exported USD directly before blaming the upstream asset."
