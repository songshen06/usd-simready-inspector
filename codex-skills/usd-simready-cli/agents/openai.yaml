interface:
  display_name: "USD SimReady CLI"
  short_description: "Process USD assets for SimReady validation."
  default_prompt: "Process this USD with the unified SimReady CLI, then verify dependencies, orientation, scale, post-export bbox size, collision, and output report. If downstream runtime output disagrees on size, verify the exported USD directly before blaming the upstream asset."
