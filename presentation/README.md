# Intro slide decks

Deliverables in **this folder** (only these need to be opened or shared):

| File | Contents |
|------|----------|
| **`usd-simready-inspector-flow.pptx`** | Two pipeline overview slides (general inspection + static furniture SimReady). |
| **`usd-simready-inspector-nvidia-template.pptx`** | NVIDIA Light External–style deck: intro, agenda, summary, cumulative-commit line chart, closing. |

To merge in PowerPoint: open the NVIDIA file → **Reuse Slides** → pick the flow `.pptx` → insert the two flow slides where you want them.

Maintainers who need to rebuild from source: everything lives under **`build/`** (HTML sources, manifest, one shell entrypoint).
